<tr>
  <td><%= image_tag(advantage.avatar_url("zoe666")) %></td>
  <td><%= advantage.content %></td>
  <td><%= advantage.url %></td>
  <td><%= advantage.created_at.strftime("%F %T")%></td>
  <td>
    <%= link_to "修改", edit_admin_advantage_path(advantage), class: "btn btn-default btn-xs" %>
    <%= link_to "删除", admin_advantage_path(advantage), method: :delete, class: "btn btn-default btn-xs" %>
  </td>
</tr>
