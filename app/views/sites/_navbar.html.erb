<!-- banner positon including navbar and site_copyright -->
  <div class="header-site">
    <div class="topNav-site">
      <!-- 显示当前公司站点的logo-->
      <a href="/">
        <img class="logo" src="<%= @current_site.avatar.url("zoe666") %>" >
      </a>
      <!-- 显示菜单内容 -->
      <div class="navBar-site">
        <ul class="nav-site clearfix-site">
          <h4>
            <% @current_site.categories.each do |category| %>
              <td><%= link_to category.menu, category.menu_url %>
              |
              </td>
            <% end %>
          </h4>
        </ul>
      </div>
    </div>
  </div>
