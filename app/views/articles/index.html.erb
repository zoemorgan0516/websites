<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="http://localhost:3000/sites/<%= @site.id %>"><%= @site.name %></a>
      </div>
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">博客首页</a></li>
        <li><a href="http://localhost:3000/emails/new">联系我们</a></li>
      </ul>
    </div>
 </nav>

<div class="col-md-12">
   <% @articles.each do |article| %>
     <div class="panel panel-default">
         <div class="panel-body">
             <h2><%= link_to(article.title, article_path(article)) %></h2>
             <h3><%= raw article.content %></h3>
          </div>
      </div>
   <% end %>
  <%= paginate @articles, outer_window: 3 %>
  <%= link_to_next_page @articles, 'Next Page' %>
  <%= page_entries_info @articles, entry_name: 'article' %>
  <%= paginate @articles %>
</div>
