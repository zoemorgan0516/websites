<aside id="aside" class="app-aside app-aside-fixed hidden-xs bg-dark">
  <div class="aside-wrap">
    <div class="navi-wrap">
      <!-- nav -->
      <nav ui-nav class="navi clearfix">
        <ul class="nav">
          <li class="hidden-folded padder m-t m-b-sm text-muted text-xs">
            <span>导航</span>
          </li>
          <li class="<%= "active" if controller_name == "home" %>">
            <a href="/admin/home" class="auto">
              <i class="glyphicon glyphicon-stats icon text-primary"></i>
              <span class="font-bold">控制面板</span>
            </a>
          </li>
          <li class="hidden-folded padder m-t m-b-sm text-muted text-xs">
            <span>平台管理</span>
          </li>

          <% if can?  :manage, Site %>
              <li class="<%= "active" if controller_name == "admin::sites" %>">
                <a href="/admin/sites" class="auto">
                  <i class="glyphicon glyphicon-blackboard"></i>
                  <span>多站点管理</span>
                </a>
              </li>
          <% elsif can? :update, Site %>
              <li class="<%= "active" if controller_name == "admin::sites" %>">
                 <a href="/admin/sites/<%= current_user.site.id %>/edit" class="auto">
                  <i class="glyphicon glyphicon-blackboard"></i>
                  <span>站点管理</span>
                </a>
              </li>
          <% end %>

          <% if can? :manage, User %>
              <li class="<%= "active" if controller_name == "users" %>">
                <a href="/managers" class="auto">
                  <i class="glyphicon glyphicon-user"></i>
                  <span>用户管理</span>
                </a>
              </li>
          <% else %>
              <li class="<%= "active" if controller_name == "users" %>">
                <a href="/managers" class="auto">
                  <i class="glyphicon glyphicon-user"></i>
                  <span>账号管理</span>
                </a>
              </li>
          <% end %>

          <% if can? :manage, Category %>
              <li class="<%= "active" if controller_name == "admin::categories" %>">
                <a href="/admin/categories" class="auto">
                  <i class="fa fa-users"></i>
                  <span>菜单分类管理</span>
                </a>
              </li>
          <% end %>

          <% if can? :read, SliderPicture %>
            <li class="<%= "active" if controller_name == "admin::slider_pictures" %>">
              <a href="/admin/slider_pictures" class="auto">
                <i class="fa fa-users"></i>
                <span>滑动图片栏管理</span>
              </a>
            </li>
          <% end %>

          <% if can? :manage, Content %>
            <li class="<%= "active" if controller_name == "admin::contents" %>">
              <a href="/admin/contents" class="auto">
                <i class="fa fa-users"></i>
                <span>文字图片内容管理</span>
              </a>
            </li>
          <% end %>


          <% if can? :manage, Advantage %>
            <li class="<%= "active" if controller_name == "admin::advantages" %>">
              <a href="/admin/advantages" class="auto">
                <i class="fa fa-users"></i>
                <span>我们的优势栏管理</span>
              </a>
            </li>
          <% end %>

          <% if can? :manage, Cooperation %>
            <li class="<%= "active" if controller_name == "admin::cooperations" %>">
              <a href="/admin/cooperations" class="auto">
                <i class="fa fa-users"></i>
                <span>合作伙伴管理</span> </a>
            </li>
          <% end %>


          <li class="line dk hidden-folded"></li>
        </ul>
      </nav>
      <!-- nav -->
    </div>
  </div>
</aside>
